diff --git a/exchange/js/exchange.js b/exchange/js/exchange.js
index e28b539..5b091e7 100644
--- a/exchange/js/exchange.js
+++ b/exchange/js/exchange.js
@@ -56,6 +56,7 @@ const DEFAULT_EDITION_COUNT = 10;
 const exchangeState = {
   hasCartItems: false,
 };
+let exchangeInFlight = false;
 
 // =============================================================================
 // GLOBAL UTILITIES
@@ -960,6 +961,9 @@ async function pollForConfirmation(opHash, timeout = 120000, interval = 5000) {
 // -----------------------------------------------------
 
 async function handleExchange() {
+  if (exchangeInFlight) return;
+  exchangeInFlight = true;
+
   try {
     console.log(`🚀 Exchange button clicked! Starting process on ${network}...`);
 
@@ -1137,6 +1141,8 @@ async function handleExchange() {
     console.error('❌ Error during exchange process:', error);
     showModal('ERROR', `${error.message || 'Unknown issue'}`);
     setTimeout(hideModal, 3000);
+  } finally {
+    exchangeInFlight = false;
   }
 
   /**
