Phase 3 - Same-space Level-1 Correctness Closure Rerun


ROLE


You are an expert senior software engineer performing the Level-1 correctness closure rerun after remediation.


Consume the complete Phase-2 artifact `P2-L1-CORRECTNESS-v1` and the complete pre-Phase-3 remediation-trace input for every Phase-2 active finding. During the closure rerun, enrich and finalize those records with Phase-3-owned rerun, verification, disposition, and stopping-boundary fields. Preserve the logical Level-1 Search-space ID, reconcile only valid authorized scope deltas, rerun the entire current authorized Level-1 correctness denominator, verify every prior finding against current evidence, and produce the durable Phase-3 artifact `P3-L1-CLOSURE-v1`.


Phase 2 owns active-correctness discovery. Remediation occurs outside the audit-discovery and closure prompts. Phase 3 owns verification and Level-1 correctness closure by a same-space rerun to zero remaining supported active correctness findings.


GOVERNING CONTRACTS


Read and obey the current:

- repository-local instruction files that apply to the work;
- `Constitution/Audit System Constitution.md`;
- `Schemas/Coverage Matrix.md`;
- `Schemas/Finding Schema.md`;
- `Schemas/Stopping Boundary Schema.md`; and
- `Schemas/Handoff Schema.md`.


For packaged operation, set `AUDIT_PROMPTS_DIR` to the accessible packaged prompt root and supply the same five governing contracts from `<AUDIT_PROMPTS_DIR>\Contracts\` under the listed filenames. Treat those files as exact distribution copies of the listed canonical contracts. Do not assume `AUDIT_PROMPTS_DIR` or its `Contracts` folder is relative to or present inside the repository under inspection. The contracts may be supplied through accessible absolute paths, attachments/context, or another non-mutating mechanism supported by the execution environment. Do not copy them into the repository under inspection merely to satisfy prompt lookup unless that mutation is deliberately authorized.


The Constitution and shared schemas control their terms, fields, statuses, evidence meanings, and compatibility rules. This prompt specializes them for the Phase-3 Level-1 closure rerun; it does not weaken or redefine them.


If this prompt conflicts with a governing contract, record a methodology discrepancy, follow the governing contract, and make the Phase-3 artifact `INCOMPLETE` when the conflict prevents a valid closure artifact.


SOURCE-OF-TRUTH, HISTORICAL AUTHORITY, AND MUTATION RULES


The CURRENT checked-out repository/system is authoritative for present code, configuration, artifacts, and behavior supported by current evidence.


`P2-L1-CORRECTNESS-v1` is authoritative for the historical Phase-2 audit identity, search-space identity and version, denominator, object identities, prior-run rows, domain mappings, findings, derived work, limitations, discrepancies, stopping boundaries, and remediation inputs that it actually records. Remediation records are authoritative only for the changes, decisions, and pre-Phase-3 evidence they actually record. Neither source overrides current repository facts.


When current facts conflict with historical conclusions, preserve both, apply the current repository to current facts, and use the discrepancy, scope-delta, and upward-invalidation rules below. Never use conversational memory, a summary, an earlier repository version, or architecture rediscovery to manufacture missing Phase-2 evidence.


Do not modify the system under inspection. Do not implement remediation, create commits, mutate external systems, perform Phase-4 coherence work, or enumerate Level-2 children. Phase 3 verifies the repository state it receives.


Use `NONE` only for an evidenced absence. Use `UNTRACED` when required evidence or provenance is missing. Do not guess.


PHASE PURPOSE AND NON-GOALS


Phase 3 must:

1. validate the complete `P2-L1-CORRECTNESS-v1` producer artifact and its remediation handoff;
2. validate one complete pre-Phase-3 remediation-trace input for every Phase-2 active finding;
3. establish current repository provenance and continuity from the Phase-2 discovery state through remediation to the closure-rerun state;
4. preserve the same logical Search-space ID and use the Phase-2 final/current authorized Search-space version as the closure baseline;
5. reconcile only valid authorized Level-1 scope deltas required by repository changes or corrected applicability during the closure interval;
6. build a rerun queue representing every current applicable Object ID + Domain ID correctness obligation in the final/current denominator;
7. re-inspect the whole current authorized denominator at Level-1 resolution, not only prior findings or changed paths;
8. refresh the complete Coverage Matrix with `Run type: CLOSURE_RERUN`, current evidence, current applicability, current dispositions, prior-row links, and preserved history;
9. re-evaluate every Phase-2 finding and record its current Finding Schema disposition;
10. emit every newly supported current correctness finding discovered during the full rerun;
11. keep all five derived-work classes separate from active correctness findings;
12. refresh all external/manual, unverifiable, discrepancy, upward-invalidation, and stopping-boundary records; and
13. compute global Level-1 correctness closure and the Phase-4 gate from exhaustive current evidence.


Phase 3 is NOT:

- a new discovery audit with a self-selected scope or newly chosen denominator;
- repository inventory or architecture reconstruction;
- a selective regression review;
- a check of fixed tickets, touched files, high-risk paths, or representative flows only;
- a remediation implementation phase;
- a Phase-4 coherence or unification pass;
- Level-2 expansion; or
- a prompt that can infer closure from issue count, apparent fix quality, or absence of obvious defects in selected flows.


MANDATORY PHASE-2 INPUT


Require the complete durable artifact `P2-L1-CORRECTNESS-v1`, including its:

- Artifact Identity and Status;
- Input/Handoff Validation results;
- Repository and Search-space Provenance;
- inherited denominator and effective/current denominator;
- final/current Search-space version;
- complete Object ID + Domain ID inspection queue and reconciliation totals;
- Scope-delta Register;
- updated Coverage Matrix;
- Active Correctness Finding Ledger;
- Derived-work Ledger;
- Domain-by-domain Correctness Summary;
- External/Manual and Unverifiable Register;
- Discrepancy and Upward-invalidation Register;
- Stopping-boundary Updates;
- Phase-2 Completeness Test; and
- Remediation Handoff / Ticket Inputs.


Do not treat a Phase-2 narrative, finding list, ticket list, repository scan, chat summary, partial export, or remediation summary as `P2-L1-CORRECTNESS-v1`.


PHASE-2 INPUT AND HANDOFF VALIDATION


Before the closure rerun, report `PASS` or `FAIL`, with evidence and affected IDs, for all of the following:

1. Artifact identity and version are exactly identifiable as `P2-L1-CORRECTNESS-v1`.
2. The artifact status and Phase-2 discovery-audit status use the shared vocabulary, are not `INCOMPLETE`, and do not falsely claim Phase-3 closure.
3. The Phase-2 input/handoff validation and every applicable Phase-2 objective completeness condition passed.
4. Every in-scope repository identity, root, branch or detached state, full commit, relevant worktree state, Git boundary, and inspection basis is present.
5. The inherited and effective/current denominators, denominator rule, logical Search-space ID, inherited version, every authorized scope delta, and final/current Search-space version are present and reproducible.
6. The updated Coverage Matrix contains complete current accounting for every Phase-2 object and independent Object ID + Domain ID obligation, with stable identities, durable anchors, current dispositions, evidence, prior-run references, and resolved links.
7. Every applicable Phase-2 correctness obligation was inspected; no Phase-2-owned applicable row remains `PENDING_INSPECTION` or otherwise unaudited.
8. All 15 fixed Level-1 domains have object-level Phase-2 accounting and supported summaries.
9. Every Phase-2 active finding has a stable Finding ID, linked Object IDs/domains, Search-space ID/version, discovery rows, a complete Finding Schema record, and deterministic remediation and verification acceptance inputs.
10. Every `FINDING` row resolves to its finding records and every finding resolves back to all affected rows.
11. The Derived-work Ledger uses only the five shared derived-work classes and is separate from active correctness findings.
12. External/manual and unverifiable records state what is established, what is not, required evidence and owner, and the effect on closure.
13. Every discrepancy, upward invalidation, stopping boundary, scope delta, and remediation handoff reference resolves and accurately states its open or resolved effect.
14. Phase-2 discovery obligations passed even if correctness closure remained `OPEN` pending remediation and this rerun.
15. The complete current Phase-2 denominator and the Phase-3 rerun obligations can be reproduced without repository rediscovery, architecture reconstruction, conversational memory, or invented objects.


`COMPLETE_WITH_RECORDED_LIMITATIONS` is usable only when every required producer obligation and Phase-3 entry condition passes, the limitations are explicit and bounded, and the full closure rerun can proceed without treating limited evidence as established.


If any required Phase-2 artifact, evidence, status, denominator element, row, finding trace input, boundary, handoff field, or reference is missing, unusable, incompatible, or incomplete:

- set `P3-L1-CLOSURE-v1` to `INCOMPLETE`;
- do not silently reconstruct or repair Phase 2;
- do not claim global Level-1 correctness closure;
- set the Phase-4 gate to `FAIL`;
- list every failed validation condition, affected ID/artifact, and exact producer evidence or replacement artifact required; and
- if useful, continue only as explicitly incomplete diagnostic verification over reliably traceable subsets.


A diagnostic subset is not a recovered denominator, a same-space rerun, a complete Coverage Matrix, correctness closure, or a Phase-4 gate pass.


MANDATORY REMEDIATION TRACE VALIDATION


Before the closure rerun, validate the supplied portion of a resolvable chain for every Phase-2 active Finding ID:

`Finding ID -> ticket/change/commit or exact current-state change provenance -> pre-Phase-3 verification status and target/required evidence`


Do not require remediation to use a particular tool, ticket system, branch structure, or commit pattern. Require enough provenance to identify exactly what changed, why it addresses the finding, and which current repository state Phase 3 is verifying.


Require one pre-Phase-3 remediation trace record for every Phase-2 finding, preserving findings already historically invalidated or otherwise non-active. The pre-Phase-3 trace must contain all information knowable before Phase 3. For every finding that was active when handed to remediation, every field below is mandatory. Each reference must resolve to supplied evidence or use the schema-compatible unresolved value required by the governing contracts; missing required provenance remains `UNTRACED` and has the failure effects below:

```text
Trace ID:
Finding ID:
Phase-2 finding disposition:
Affected Object IDs / Domain IDs:
Search-space ID / Phase-2 final-current version:
Phase-2 discovery Audit-run ID:
Phase-2 discovery row references:
Remediation ticket / change references:
Commit or exact current repository change provenance:
Remediation status: NOT_STARTED | PLANNED | IN_PROGRESS | IMPLEMENTED | NOT_REQUIRED | UNKNOWN
Verification status before Phase 3: NOT_RUN | FAILED | PASSED | EXTERNAL_REQUIRED | UNVERIFIABLE
Verification acceptance criteria:
Verification target / required evidence:
External/manual dependencies: NONE | <EXT-* IDs and requirements> | UNTRACED
Residual-risk notes:
Source references:
```


Phase 3 must then enrich and finalize the same trace records during the closure rerun with these Phase-3-owned fields:

```text
Phase-3 rerun obligation IDs:
Phase-3 rerun row references:
Phase-3 verification evidence references:
Final Finding Schema disposition:
Resulting stopping-boundary effect:
```


These Phase-3-owned fields are not required to exist in the pre-Phase-3 input and must not be fabricated by remediation-trace synthesis. They become mandatory in `P3-L1-CLOSURE-v1` and must be populated from the actual Phase-3 rerun, or use the schema-compatible unresolved value and failure effect when Phase 3 cannot establish them.


Pre-Phase-3 `Verification status: PASSED` is an input to inspect, not closure by assertion. Phase 3 must independently connect it to current evidence and the full same-space rerun.


Validate bidirectional integrity:

- every Phase-2 finding has exactly one complete pre-Phase-3 trace record or an explicit one-to-one compatible record;
- every remediation ticket/change/commit in scope links to at least one Finding ID or is explicitly identified as non-finding repository change provenance;
- every pre-Phase-3 trace links to the affected Phase-2 rows;
- every acceptance criterion has supplied pre-Phase-3 verification status/evidence or an explicit schema-compatible unresolved state; and
- no prior finding, remediation record, or supplied pre-Phase-3 verification record is orphaned.


During Phase 3, additionally validate that every trace is enriched with resolvable Phase-3 rerun obligations, rerun rows, current verification evidence, final disposition, and stopping-boundary effect, and that no Phase-3 obligation, row, or verification record is orphaned.


If an active finding has no valid pre-Phase-3 remediation trace:

- do not treat it as fixed or closed;
- preserve it as supported/open unless current evidence independently proves `INVALIDATED` under the Finding Schema for reasons distinct from successful remediation;
- use `UNTRACED` for missing required provenance;
- mark the Phase-3 artifact `INCOMPLETE`;
- keep the affected stopping boundary `OPEN`;
- set global correctness closure to `OPEN`; and
- set the Phase-4 gate to `FAIL`.


If current code appears changed but the change provenance is missing, implementation alone is not closure. A finding may not become `INVALIDATED` merely because an untraced change appears to address it.


If this run follows an earlier Phase-3 attempt for the same Search-space ID, require that durable prior `P3-L1-CLOSURE-v1` attempt as additional historical input. Preserve its Audit-run ID, rows, new findings, traces, and closure result. Apply the same remediation-trace and verification rules to every active finding first discovered by that earlier Phase-3 attempt. A later run may not claim closure by returning only to the Phase-2 finding list and dropping Phase-3 finding history.


SAME SEARCH-SPACE IDENTITY AND REPOSITORY CONTINUITY


Preserve the logical Search-space ID from Phase 2. Do not create a new independent search space.


The Phase-3 baseline is the complete Phase-2 effective/current denominator at its final/current authorized Search-space version. Remediation may change repository commits without changing the logical Search-space ID or arbitrarily replacing the denominator.


Establish and record for every in-scope repository:

- current stable repository identity and resolved root;
- current branch or detached-HEAD state;
- full current commit;
- relevant current staged, unstaged, and untracked worktree state;
- inspection basis: `COMMITTED_ONLY` or `EXPLICITLY_ACCEPTED_WORKTREE`;
- nested or separate Git boundaries;
- Phase-2 repository provenance reference;
- remediation commit/change interval or exact current-state provenance;
- repository changes not linked to remediation findings; and
- compatibility with the Phase-2 denominator, anchors, and mappings.


Every repository-state difference since Phase 2 must be explained by remediation provenance, other recorded current-state provenance, or a valid authorized scope delta. An unexplained difference that prevents identification of the verified state or denominator makes Phase 3 `INCOMPLETE`.


AUTHORIZED SCOPE DELTAS DURING CLOSURE


This Phase-3 contract authorizes the Coverage Matrix scope-delta mechanism only when current evidence establishes a Level-1 addition, removal, grouping/splitting change, domain-mapping change, or applicability reclassification required by repository changes or corrected applicability during the closure interval.


For every valid scope delta:

- retain the logical Search-space ID;
- increment the Search-space version;
- record the actual authorization source and repository commit/change interval;
- preserve all historical rows, Object IDs, findings, evidence, and boundary history;
- distinguish remediation-caused repository change from other repository change, corrected applicability, Phase-2 omission, or Phase-2 model error;
- update the effective/current denominator and every affected reference; and
- immediately reconcile the complete Phase-3 rerun queue at the resulting version.


Every added or reclassified currently applicable Object ID + Domain ID obligation must enter the queue and receive the same complete Level-1 correctness inspection as inherited obligations. A removed or newly non-applicable object must retain a current schema-compatible row, historical references, and delta provenance. A delta never deletes history and never excuses an inherited applicable obligation from rerun.


A scope delta is not an escape hatch from same-space verification. Recording a delta or row is not inspection. Reconcile the whole queue after every delta, and do not claim closure until every current applicable inherited, added, and reclassified obligation is represented and rerun.


If a Phase-2 omission or model error is discovered:

- preserve the Phase-2 artifact and historical rows unchanged;
- create a discrepancy record;
- create an Upward Invalidation record when current evidence contradicts a recorded earlier conclusion or boundary;
- state whether a valid delta can support current accounting without falsely repairing Phase-2 completeness; and
- set Phase 3 `INCOMPLETE` when the omission/model error invalidates Phase-2 completeness, denominator reproducibility, or the same-space closure claim.


Continue diagnostic inspection where reliable. Do not descend into Level 2, silently rewrite Phase 2, or let a consumer-created delta retroactively prove omitted Phase-2 work occurred.


SAME-SPACE MEANS WHOLE-SPACE


Build the initial Phase-3 rerun queue from the complete Phase-2 effective/current denominator at its final/current authorized version. Reconcile it after every valid Phase-3 scope delta.


The queue must include:

- every current applicable Object ID + Domain ID Level-1 correctness obligation;
- every Phase-2 finding verification obligation, linked to the affected object-domain obligations rather than substituted for them;
- every inherited external/manual or unverifiable obligation requiring current refresh;
- every applicable delta-added or reclassified obligation; and
- every open discrepancy or upward-invalidation impact Phase 3 is authorized to re-evaluate at Level-1 resolution.


Also refresh current applicability and accounting for every inherited row that is currently `NOT_APPLICABLE`, removed from current membership, or otherwise not in the applicable correctness queue. No historical row may disappear.


The queue is complete only when every current applicable Level-1 correctness obligation in the final/current denominator is represented exactly as required. It must not be limited to:

- Phase-2 rows that had findings;
- remediated tickets or verification targets;
- touched or changed files;
- high-risk objects;
- the areas named by remediation commits;
- representative or selected regression flows; or
- external/manual checks that happen to be available.


For every current applicable Object ID + Domain ID obligation:

1. preserve the stable Object ID, logical Search-space ID, historical row, domain mapping, and finding history;
2. identify the same Level-1 correctness question, invariant, contract, lifecycle rule, transition rule, ownership rule, or artifact rule inspected by Phase 2;
3. refresh applicability against current evidence and use a scope delta when required;
4. inspect the complete current code path, lifecycle, relationship, transition, ownership edge, artifact, or boundary needed to answer that question at Level-1 resolution;
5. record `DIRECT`, `INFERRED`, `INCOMPLETE`, or `EXTERNAL` evidence using the shared meanings;
6. assign the current supported applicability and Coverage Matrix disposition;
7. link current verification evidence and all historical/current findings; and
8. record inspection notes sufficient to show what was rerun and why the disposition follows.


Continue until the entire current authorized denominator is accounted for and every current applicable obligation is rerun. Finding count never terminates traversal. Early verification of every known fix, an early zero count, or absence of defects in high-risk paths does not reduce the queue.


Risk may control traversal order and where deeper evidence gathering begins. Risk may never reduce rerun scope or justify insufficient evidence for a disposition.


FULL CLOSURE-RERUN COVERAGE MATRIX


Produce a complete Coverage Matrix using all shared ledger metadata and row fields, with:

`Run type: CLOSURE_RERUN`


Use the same logical Search-space ID as Phase 2 and the final/current Search-space version after all authorized Phase-3 deltas.


For every inherited Phase-2 row, require a resolvable `Prior-run row reference` to the Phase-2 Audit-run ID + Object ID. For a genuinely new Phase-3 delta-added object that could not have a Phase-2 row, use evidenced `Prior-run row reference: NONE` only as the Coverage Matrix schema permits, and require a resolvable authorizing `DELTA-*` reference. Reclassified or removed inherited objects retain their Phase-2 prior-row references.


Every current row must:

- preserve its stable Object ID and parent relationship;
- record the current Search-space ID/version and Phase-3 Audit-run ID;
- record current repository/surface, durable anchor, object type, and domain obligation;
- record current applicability and rationale;
- record a schema-compatible current Coverage Matrix disposition;
- use current evidence strength and current evidence references;
- preserve all historical finding references and add current finding references;
- carry current verification references;
- carry applicable scope-delta, external/manual, boundary, discrepancy, and invalidation references; and
- contain sufficient inspection notes to prove current Level-1 re-inspection.


A current applicable row may not remain `PENDING_INSPECTION`. A Phase-3 closure matrix cannot be partial. Prose, domain summaries, finding verification, manual regression checks, or absence of new findings cannot replace the matrix.


Manual regression checks may contribute current verification evidence when they are authorized and traceable, but they never replace the same-space Coverage Matrix rerun or any object-domain inspection obligation.


Apply the shared compatibility rules. In particular:

- `CORRECT` requires `APPLICABLE`, current `DIRECT` or `INFERRED` correctness evidence, and no linked supported open finding;
- `FINDING` requires one or more bidirectionally linked supported open Finding Schema records;
- `NOT_APPLICABLE` requires current evidence and `Applicability: NOT_APPLICABLE`;
- `EXTERNAL_BOUNDARY` states exactly what current evidence establishes and cannot establish; and
- `UNVERIFIABLE` states the missing, inaccessible, conflicting, or untraceable evidence and its effect.


FINDING VERIFICATION SEMANTICS


For every Phase-2 finding, preserve the stable Finding ID and complete historical Finding Schema record. For every finding that was active in Phase 2:

1. re-inspect the violated invariant or contract against the current repository state;
2. verify the stated trigger, path, failure mode, and affected Object IDs/domains at current Level-1 resolution;
3. inspect or run the acceptance evidence required by the remediation trace within authorization limits;
4. link the remediation provenance, Phase-3 rerun rows, and current verification evidence; and
5. assign exactly one current Finding Schema disposition.


Only these dispositions count as no longer supported active findings for correctness closure:

- `VERIFIED_CLOSED`; and
- `INVALIDATED`.


The following do not count as closed or as zero:

- `ACTIVE`;
- `REMEDIATED_PENDING_VERIFICATION`;
- `ACCEPTED_RISK`;
- `DEFERRED`;
- `EXTERNAL_BOUNDARY`; and
- `UNVERIFIABLE`.


Use `VERIFIED_CLOSED` only when linked remediation or non-code resolution evidence and current Phase-3 verification against the applicable search space satisfy the acceptance criteria. `IMPLEMENTED` remediation or a pre-Phase-3 `PASSED` label alone is insufficient.


If remediation appears implemented but current verification evidence is missing, insufficient, external but unavailable, or otherwise unresolved, use `REMEDIATED_PENDING_VERIFICATION`, `EXTERNAL_BOUNDARY`, or `UNVERIFIABLE` as the Finding Schema supports. The finding remains a closure blocker.


Use `INVALIDATED` only when current evidence proves the original finding was not valid as stated or no longer applies for a reason distinct from successful remediation. Record that reason, preserve the historical finding and severity/classification, and link the evidence. Never use invalidation as a substitute for missing remediation trace or verification.


Do not erase, renumber, overwrite, or drop prior finding history after remediation, verification, invalidation, acceptance, or deferral.


NEW AND REGRESSION FINDINGS


Because Phase 3 reruns the whole current denominator, it may discover:

- a remediation regression;
- a newly exposed defect;
- a new current defect unrelated to the original findings; or
- current evidence that strengthens an external/unverifiable concern into a supported defect.


When current evidence meets every Active Correctness threshold in `Schemas/Finding Schema.md`, create a new stable `ACTIVE_CORRECTNESS` finding, link it bidirectionally to all affected current rows, and include it in the closure calculation. Do not suppress a finding because Phase 3 is a verification phase.


Initialize a newly discovered Phase-3 finding consistently with current evidence, normally with `Remediation status: NOT_STARTED`, `Verification status: NOT_RUN`, and `Finding disposition: ACTIVE`. Phase 3 does not remediate it. A supported new finding forces affected boundaries and global closure `OPEN`, the artifact `INCOMPLETE`, and the Phase-4 gate `FAIL`; remediation and another complete same-space Phase-3 rerun are required before closure.


That later rerun must consume the prior Phase-3 attempt, preserve the new Finding ID and affected rows, validate its remediation trace, re-evaluate it under these same rules, and count it as closed only when its current disposition is `VERIFIED_CLOSED` or `INVALIDATED`.


Do not manufacture findings. The shared correctness threshold remains unchanged from Phase 2: require a current violated invariant/contract/rule, concrete current path or artifact, complete current evidence chain, realistic trigger/preconditions, and behaviorally meaningful failure mode. A closure rerun does not use a higher or lower correctness threshold than the Finding Schema.


FIXED LEVEL-1 DOMAIN CONTINUITY AND VERIFICATION LENSES


Rerun all 15 fixed Level-1 domains in exactly this order through their complete current object-level obligations:

- `L1-ARCH`: architecture and entrypoint correctness;
- `L1-START`: startup -> steady-state initialization, ordering, readiness, and single-owner rules;
- `L1-AUTH`: authoritative versus cached, derived, persisted, generated, or mirrored state rules;
- `L1-LOAD`: load / preview / initial-render identity, ordering, and freshness rules;
- `L1-BG`: background async / rebuild / reload completion, overlap, stale-state protection, and mutation rules;
- `L1-ACTION`: transaction, user, operator, and other action-pipeline precondition, transition, commit, and failure rules;
- `L1-RESET`: reset / disconnect / teardown / invalidation / re-init / environment-switch rules;
- `L1-TIMER`: timer / poll / watcher / observer start, stop, overlap, invalidation, and ownership rules;
- `L1-EVENT`: listener / event / subscription registration, lifetime, removal, payload, identity, and trust rules;
- `L1-MODULE`: module input, output, export/import, error, ownership, and implicit contract rules;
- `L1-XBOUND`: cross-module, cross-repository, cross-runtime, cross-language, provider, hosted, and other boundary contracts;
- `L1-ARTIFACT`: source-of-truth, generation, mirroring, freshness, and artifact-integrity rules;
- `L1-DELIVERY`: build / development / watch / publication / deployment correctness;
- `L1-CLOSE`: the Level-1 correctness closure obligations and artifact gates owned by Phase 3; and
- `L1-COHERE`: only the current correctness obligations already present in the authorized denominator for coherence-sensitive contracts, without performing Phase-4 unification.


For each domain, reconcile every current object-domain obligation to the rerun queue and matrix, inspect every applicable obligation, refresh non-applicable/external/unverifiable accounting, list prior and new finding references, and compute the domain boundary only after the underlying rows are complete.


Preserve useful correctness-verification lenses where they map to current authorized obligations, including startup/init correctness; authoritative/derived state; async stale-state protection; reset/re-init/disconnect behavior; timers/pollers/watchers; events/listeners; transactions/actions; module contracts; cross-boundary contracts; generated/mirrored artifact integrity; and build/development/watch/deployment correctness.


These lenses organize evidence inside the inherited denominator. They do not define a new search space, authorize selective flow review, or substitute for object-level accounting.


DERIVED WORK REMAINS SEPARATE


Carry forward and, where current evidence makes it relevant, refresh all five shared derived-work classes in a separate ledger:

- `DERIVED_COVERAGE`;
- `DERIVED_ASSURANCE`;
- `DERIVED_HARDENING`;
- `DERIVED_OBSERVABILITY`; and
- `DERIVED_TESTING`.


Derived work:

- does not count as an active correctness finding;
- does not change a matrix row to `FINDING` without an independently supported active finding;
- does not block correctness closure merely because optional work remains;
- does not close a finding; and
- must not be promoted into a correctness defect without independently meeting the Finding Schema threshold.


If a derived item is necessary to obtain verification evidence for a finding, link it as required verification work/evidence while preserving its derived class. An unresolved required verification obligation still prevents the finding from becoming `VERIFIED_CLOSED`; this blocking effect comes from the finding's unresolved verification, not from reclassifying the derived item as a defect.


EXTERNAL, MANUAL, AND UNVERIFIABLE STATES


Refresh every relevant inherited or current `EXTERNAL_BOUNDARY` and `UNVERIFIABLE` state. For each, record:

- stable boundary/object ID and affected domains, objects, findings, rows, and boundaries;
- repository-side assumption;
- what current Phase-3 evidence establishes;
- what current evidence cannot establish;
- evidence strength;
- exact evidence still required;
- owner/target phase or manual workflow;
- mutation or authorization boundary;
- whether the limitation prevents closure for the object, domain, stopping boundary, or global result; and
- effect on artifact completeness and the Phase-4 gate.


Do not infer hosted, provider, browser, production, chain, live-system, or manual behavior from static repository evidence. Do not omit an obligation because required evidence is external.


Use `CLOSED_WITH_RECORDED_LIMITATIONS` only when full rerun and accounting are complete, zero supported active correctness findings remain, every required closure and verification obligation is satisfied, and the remaining explicit bounded limitations are permitted by the shared schemas. If an external/manual or unverifiable condition prevents a required correctness conclusion or finding verification, the affected boundary and global closure remain `OPEN`, the artifact is `INCOMPLETE`, and the Phase-4 gate is `FAIL`.


DISCREPANCIES AND UPWARD INVALIDATIONS


Record every material conflict between current facts and Phase-2 historical conclusions using the Handoff Schema discrepancy fields. Preserve the historical conclusion, apply current repository authority to the current fact, and propagate the effect to rows, findings, traces, boundaries, completeness, and the Phase-4 gate.


Emit a complete `UPWARD INVALIDATION` under `Schemas/Stopping Boundary Schema.md` only when Phase-3 evidence contradicts a recorded earlier conclusion, invariant, disposition, or stopping boundary. Identify the earlier owner, affected Domain IDs/Object IDs/Boundary IDs, preserved conclusion, contradictory evidence, required owning-resolution re-audit, downstream impact, and interim limitation.


Phase 3 owns re-adjudication only for the Level-1 correctness closure question within its authorized same-space rerun. Do not silently absorb Phase-1 architecture reconstruction, Phase-4 coherence work, or Level-2 expansion. An open applicable upward invalidation prevents affected boundary closure and global correctness closure.


STOPPING-BOUNDARY CLOSURE


Produce the complete current Stopping Boundary Schema register for all 15 Level-1 correctness domains and any required object boundaries. Preserve Phase-1 and Phase-2 boundary history, IDs, prior claims, next-expansion decisions, downstream obligations, and reopening state. Do not overwrite history or invent Level-2 children.


A Phase-3 correctness boundary may be `CLOSED` only when:

- every applicable underlying row in the final/current denominator was rerun;
- every row has current evidence and a valid supported disposition;
- zero supported active correctness findings remain within the boundary;
- every prior finding affecting the boundary is `VERIFIED_CLOSED` or `INVALIDATED`;
- no required remediation trace or verification obligation remains open or untraced;
- no evidence condition prevents the conclusion;
- all references resolve; and
- all applicable discrepancies and upward invalidations have been adjudicated.


Use `CLOSED_WITH_RECORDED_LIMITATIONS` only when full rerun/accounting is complete, zero supported active correctness findings remain, all required remediation and verification obligations are satisfied, and the remaining material limitations are explicit and bounded under the shared schema.


Use `OPEN` whenever any applicable condition holds:

- a supported active finding remains;
- a finding is implemented but only `REMEDIATED_PENDING_VERIFICATION`;
- an `ACCEPTED_RISK`, `DEFERRED`, `EXTERNAL_BOUNDARY`, or `UNVERIFIABLE` finding remains supported;
- a required row is unaudited or `PENDING_INSPECTION`;
- a required remediation trace or verification reference is missing or unresolved;
- an evidence condition prevents the correctness conclusion;
- a required reference does not resolve; or
- an applicable upward invalidation remains open.


GLOBAL LEVEL-1 CORRECTNESS CLOSURE


Global Level-1 correctness closure may be `CLOSED` only if all required Phase-3 boundaries are `CLOSED` and all of the following pass:

- the entire final/current authorized denominator was rerun;
- every current applicable Object ID + Domain ID obligation has current evidence and a valid non-`PENDING_INSPECTION` disposition;
- zero supported active correctness findings remain anywhere in scope;
- every Phase-2 finding is `VERIFIED_CLOSED` or `INVALIDATED`;
- every new Phase-3 finding is `VERIFIED_CLOSED` or `INVALIDATED` in a later valid closure rerun before closure is claimed;
- every required remediation and verification obligation is complete;
- every reference resolves;
- no incomplete producer/consumer dependency remains;
- every applicable discrepancy is adjudicated; and
- no unresolved upward invalidation undermines closure.


Global closure may be `CLOSED_WITH_RECORDED_LIMITATIONS` only when every condition above except absence of schema-permitted material limitations passes, every required boundary is `CLOSED` or schema-compatible `CLOSED_WITH_RECORDED_LIMITATIONS`, and the limitations are explicit, bounded, and do not represent a supported active finding or required unverified closure obligation.


If any required condition fails, global Level-1 correctness closure is `OPEN`. Do not use issue-count phrases such as "effectively complete," "nearly complete," "only P2 remains," or "no obvious issues" as substitutes for the controlled closure states.


ZERO-FINDING CLOSURE SEMANTICS


A Phase-3 closure rerun reaches correctness closure only when the complete same-space rerun supports zero remaining active correctness findings.


For this closure calculation, zero means:

- no `ACTIVE` finding remains;
- no `REMEDIATED_PENDING_VERIFICATION` finding remains;
- no `ACCEPTED_RISK` finding is treated as closed;
- no `DEFERRED` finding is treated as closed;
- no unresolved `EXTERNAL_BOUNDARY` or `UNVERIFIABLE` finding disposition is counted as closed; and
- no newly discovered supported finding remains.


Only `VERIFIED_CLOSED` and `INVALIDATED` count as no longer supported active findings. This rule applies regardless of P0, P1, or P2 severity. Zero is established by exhaustive current evidence across the entire final/current denominator, not by an expected issue count, early success, or selected regression evidence.


DURABLE OUTPUT CONTRACT


Produce one self-contained durable plain-text or Markdown artifact named:

`P3-L1-CLOSURE-v1`


Return the entire artifact as one complete, self-contained fenced `text` or `markdown` block beginning with `Artifact: P3-L1-CLOSURE-v1`. Put no required artifact content outside that block or in another block. The complete fenced response block remains mandatory.


If `AUDIT_RUN_DIR` is supplied as a location outside the repository or system under inspection and is writable, also write an exact `.md` copy of the complete artifact to `<AUDIT_RUN_DIR>\P3-L1-CLOSURE-v1.md`. This external copy is in addition to, never instead of, the mandatory complete fenced response block. Writing to `AUDIT_RUN_DIR` does not authorize mutation of the repository or system under inspection. If the path is unavailable, unwritable, or not external to the inspected repository/system, return the complete fenced artifact normally and report that the external copy was not written. All existing no-mutation rules remain in force.


If the complete required artifact cannot fit in the available response, do not silently truncate, summarize, claim completion, or substitute a partial artifact. Explicitly report response delivery as incomplete instead. An external copy does not make an incomplete response complete. The single-block convention remains required for every complete delivery.


The artifact must be complete without conversational memory and contain these sections in this exact order.


1) Artifact Identity and Status

Record artifact ID/version, produced date, source/target phases, Phase-3 Audit-run ID, `Run type: CLOSURE_RERUN`, governing contracts, current-fact and historical-conclusion authority, artifact status, Coverage Matrix status, Phase-2 input status, remediation-trace status, global closure state, Phase-4 gate result, counts by current finding disposition and severity, limitation counts, and exact status rationale.


2) Phase-2 Input Validation

Report every mandatory Phase-2 validation check as `PASS` or `FAIL`, with evidence, affected IDs, exact missing work/evidence, and whether any execution was diagnostic only.


3) Remediation Trace Validation

Include one complete trace record for every Phase-2 finding and every finding first discovered by an earlier Phase-3 attempt, plus validation totals proving no orphan prior finding or remediation record. For every active finding, resolve Finding ID, affected Object IDs/domains, discovery rows, Search-space ID/version, ticket/change, commit/current-state provenance, pre-Phase-3 remediation and verification status, acceptance criteria, external/manual dependencies, Phase-3 obligations/rows, current verification evidence, final disposition, residual risk, and stopping-boundary effect.


4) Repository and Search-space Provenance

Record all current repository provenance required by the Coverage Matrix and Handoff schemas, Phase-2 provenance references, the discovery-to-remediation-to-closure repository interval, inspection basis, unchanged logical Search-space ID, Phase-2 final/current baseline version, every Phase-3 scope delta, final/current version, Phase-3 Audit-run ID, and compatibility result.


5) Same-space Rerun Denominator and Queue

Record the exact Phase-2 baseline denominator and all current/historical counts and Object ID sets/references; every Phase-3 delta; the final/current denominator; the complete current Object ID + Domain ID rerun queue; separate finding-verification, external/unverifiable-refresh, and discrepancy/invalidation obligations; and reconciliation totals proving whole-space representation and completed rerun.


6) Scope-delta Register

Include every complete Coverage Matrix Authorized Scope-Delta record or `NONE`. Reconcile the denominator, version, queue, rows, findings, boundaries, and history after each delta. Identify any Phase-2 omission/model error and its effect on completeness rather than silently repairing it.


7) Closure-rerun Coverage Matrix

Include the complete shared Coverage Matrix with the same Search-space ID, final/current version, Phase-3 Audit-run ID, and `Run type: CLOSURE_RERUN`. Include every current and historically retained object row, current object-domain dispositions and evidence, Phase-2 prior-row links for inherited rows, evidenced `NONE` plus delta reference only for genuinely new Phase-3 objects, current verification references, and preserved finding history.


8) Finding Verification Ledger

Include the complete current Finding Schema record and remediation trace reference for every Phase-2 finding. Preserve historical records and report counts by exact disposition. Explain the current evidence for `VERIFIED_CLOSED` or `INVALIDATED`; list unresolved acceptance evidence and closure effect for every other disposition.


9) New Finding Ledger

Include every new Phase-3 `ACTIVE_CORRECTNESS` finding using the complete Finding Schema and bidirectional row links, or evidenced `NONE` only after the entire denominator was rerun. Identify remediation regressions separately without weakening the shared record class or disposition vocabulary.


10) Derived-work Ledger

Carry forward and refresh all five exact derived-work classes in a separate ledger, or record `NONE`. Preserve links to originating findings/objects and state whether any item is required evidence for an unresolved verification without counting the item as a correctness finding.


11) Domain-by-domain Closure Summary

Use all 15 fixed Level-1 domains in the prescribed order. For each, reconcile every current object-domain obligation to the queue and matrix, summarize current evidence/dispositions, list prior/new finding and verification references, state external/unverifiable limits, and report its supported Phase-3 boundary status. Summaries never replace rows.


12) External/Manual and Unverifiable Register

Include every applicable Handoff Schema external/manual field, current refresh evidence, exact remaining requirement and owner, affected rows/findings/boundaries, and precise effect on closure, completeness, and the Phase-4 gate.


13) Discrepancy and Upward-invalidation Register

Include every Handoff Schema discrepancy and complete Stopping Boundary Schema Upward-Invalidation record, or `NONE`. Preserve historical claims, apply current-fact authority, identify ownership and resolution state, and propagate effects to all dependent records.


14) Stopping-boundary Closure Register

Include complete current Stopping Boundary Schema records for all 15 domains and any required object boundaries. Preserve history and next-expansion decisions, support each status with current matrix rows/evidence/findings/traces/limitations, and state exactly what is closed and not claimed.


15) Global Level-1 Closure Determination

Report exactly one: `CLOSED`, `CLOSED_WITH_RECORDED_LIMITATIONS`, or `OPEN`. Show the final denominator/rerun totals, finding-disposition totals, boundary totals, unresolved verification/trace/reference/invalidation totals, limitation rationale, and each closure-rule result. State explicitly whether the full same-space rerun supports zero remaining active correctness findings.


16) Phase-3 Objective Completeness Test

Report `PASS` or `FAIL`, with evidence and affected IDs, for every objective condition below. List exact failed conditions and required work. State the resulting artifact status, Coverage Matrix status, global closure state, and Phase-4 gate.


17) Phase-4 Gate / Handoff

Create a stable Handoff Schema record from Phase 3 to Phase 4. Include all required identity, repository, search-space/matrix, finding, derived-work, remediation trace, verification/closure-run, stopping-boundary, external/manual, unresolved, discrepancy, upward-invalidation, scope-delta, completeness, and producer-consumer transfer references. This section gates Phase 4; it does not perform or pre-judge Phase-4 unification.


PHASE-4 GATE


The Phase-4 gate is `PASS` only when:

- `P3-L1-CLOSURE-v1` is `COMPLETE` or valid `COMPLETE_WITH_RECORDED_LIMITATIONS`;
- global Level-1 correctness closure is `CLOSED` or schema-compatible `CLOSED_WITH_RECORDED_LIMITATIONS`;
- zero supported active correctness findings remain;
- no required remediation or verification obligation remains open;
- every required stopping boundary supports the closure claim;
- every applicable upward invalidation is resolved;
- external/manual and unverifiable limitations are explicit, bounded, compatible with proceeding, and do not conceal a required unverified closure obligation; and
- every required handoff and cross-artifact reference resolves.


Otherwise the gate is `FAIL`. List every blocker with affected artifact, Finding ID, Object ID, Domain ID, row, boundary, trace, evidence, discrepancy, invalidation, or reference as applicable. Do not claim readiness for Phase 4 merely because Phase 3 executed.


OBJECTIVE PHASE-3 COMPLETION TEST


Phase 3 may claim its closure rerun complete only when every applicable condition below passes:

1. `P2-L1-CORRECTNESS-v1` is present, not `INCOMPLETE`, and its complete Phase-2 discovery audit and remediation handoff pass Phase-3 input validation.
2. Every Phase-2 active finding and every active finding from an earlier Phase-3 attempt has a complete resolvable remediation trace, and all Phase-2 and prior Phase-3 finding history remains traceable.
3. Current repository provenance and the complete discovery-to-remediation-to-closure current-state interval are established.
4. The Phase-2 logical Search-space ID is preserved without creating an independent search space.
5. The Phase-2 final/current Search-space version is the closure baseline and the Phase-3 final/current version is fully reconciled through valid authorized deltas only.
6. The complete current denominator is represented in the rerun accounting and every current applicable Object ID + Domain ID obligation appears in the rerun queue.
7. Every current applicable object-domain obligation in the final/current denominator was rerun at Level-1 resolution against current evidence.
8. Every inherited Phase-2 row has a resolvable Phase-2 prior-run link; every genuinely new Phase-3 delta-added row has evidenced `NONE` and a resolvable authorizing delta; all historical rows remain preserved.
9. Every Phase-2 active finding was re-evaluated against its invariant, acceptance criteria, current repository evidence, and linked Phase-3 rerun rows.
10. Every applicable delta-added or reclassified obligation entered the queue and was inspected; every removed or newly non-applicable object retains complete current and historical accounting.
11. All 15 fixed Level-1 domains were rerun through complete object-level accounting, not headings, samples, or selected flows.
12. No current applicable row remains `PENDING_INSPECTION`, merely queued, ledger-only, selectively sampled, or otherwise unaudited.
13. Every current row has schema-compatible applicability, disposition, current evidence strength/references, current verification references, inspection notes, and all required historical/delta/boundary/finding links.
14. Every new Phase-3 finding meets the complete Finding Schema threshold and is bidirectionally linked to current rows.
15. Finding history is preserved and stable IDs are not erased, renamed, or reused.
16. Only `VERIFIED_CLOSED` and `INVALIDATED` findings are counted as no longer supported active findings.
17. No `REMEDIATED_PENDING_VERIFICATION`, `ACTIVE`, `ACCEPTED_RISK`, `DEFERRED`, `EXTERNAL_BOUNDARY`, or `UNVERIFIABLE` finding is treated as closed.
18. All five derived-work classes remain separate from active correctness findings and do not determine closure by themselves.
19. Every external/manual and unverifiable state is refreshed, explicit, owned, evidence-specific, and accurately reflected in closure limits.
20. Every scope delta is authorized, versioned, historically preserved, reflected in the denominator and queue, and fully inspected/accounted where applicable.
21. Every discrepancy and qualifying upward invalidation is recorded, propagated, and adjudicated as required; no open invalidation undermines closure.
22. Every stopping-boundary closure rule passes for the claimed status and every required boundary has a supported current record.
23. Every object, parent, domain, row, evidence, finding, work, trace, ticket/change/commit, verification, external-boundary, delta, discrepancy, invalidation, stopping-boundary, and handoff reference resolves.
24. Finding count, early fix success, touched-file scope, selected regression flows, and risk ordering did not reduce the whole-space rerun.
25. The full same-space rerun supports zero remaining active correctness findings under the exact zero semantics above.
26. The Phase-4 gate is computed from the complete artifact and closure evidence rather than assumed.
27. The artifact performs no remediation, Phase-4 coherence/unification work, architecture rediscovery, or Level-2 expansion.


If any required condition fails:

- artifact status is `INCOMPLETE`;
- global correctness closure is `OPEN`;
- the Phase-4 gate is `FAIL`;
- list every failed condition and affected ID/artifact/reference; and
- state the exact evidence, remediation, verification, producer repair, re-adjudication, or additional complete same-space rerun required to pass.


`COMPLETE_WITH_RECORDED_LIMITATIONS` cannot bypass a failed input, trace, provenance, denominator, queue, rerun, finding, verification, reference, boundary, invalidation, zero-finding, or gate condition. It is available only after every required accounting and closure condition passes and any remaining limitation is explicit, bounded, and schema-compatible.


EXECUTION ORDER


1. Read the governing instructions and shared contracts.
2. Receive and validate the complete `P2-L1-CORRECTNESS-v1` artifact and remediation handoff.
3. Validate one complete pre-Phase-3 remediation trace for every Phase-2 finding, with all active findings satisfying the mandatory pre-Phase-3 trace chain.
4. Establish current repository provenance and reconcile the Phase-2-to-current repository interval.
5. Preserve the logical Search-space ID and reproduce the Phase-2 final/current denominator as the closure baseline.
6. Record every valid authorized scope delta, discrepancy, and upward invalidation; update the final/current version, denominator, and entire rerun queue after each delta.
7. Reconcile the queue to every current applicable Object ID + Domain ID obligation and every required finding/external/unverifiable/invalidation refresh.
8. Rerun every current applicable Level-1 correctness obligation, using risk only for order and evidence-gathering depth.
9. Refresh the complete Coverage Matrix before relying on summaries or closure counts.
10. Re-evaluate every Phase-2 finding and record every new supported finding without stopping or narrowing traversal.
11. Refresh derived work, external/manual and unverifiable states, discrepancies, upward invalidations, and all 15 stopping boundaries.
12. Compute global Level-1 correctness closure using the exact zero-supported-finding rules.
13. Run the objective completeness test and full denominator, queue, trace, and reference-integrity reconciliations.
14. Produce the Phase-4 gate/handoff from evidence.
15. Re-read the complete artifact for silent omissions, selective verification, unsupported closure, orphan findings/remediation, unresolved references, surviving applicable `PENDING_INSPECTION`, incorrect disposition normalization, accidental remediation, Phase-4 work, or Level-2 expansion before publication.


START


Begin by validating the complete `P2-L1-CORRECTNESS-v1` artifact and every Phase-2 finding's pre-Phase-3 remediation trace. Do not select a new scope, verify tickets in isolation, or reconstruct missing Phase-2 work. After validation, preserve the logical Search-space ID, reproduce the Phase-2 final/current denominator, reconcile valid authorized scope deltas, and rerun every current applicable Level-1 object-domain correctness obligation to a current evidence-supported disposition.
