diff --git a/drops/js/events.js b/drops/js/events.js
index 9ebe53c..2031736 100644
--- a/drops/js/events.js
+++ b/drops/js/events.js
@@ -1614,6 +1614,7 @@ function commitWalletTokenRegion({
   if (heading) {
     heading.textContent = headingVisible ? headingText : '';
     heading.style.visibility = headingVisible ? 'visible' : 'hidden';
+    heading.style.display = headingVisible ? 'block' : 'none';
   }
 
   if (spinner) spinner.style.display = spinnerVisible ? 'flex' : 'none';
diff --git a/exchange/js/exchange.js b/exchange/js/exchange.js
index 3feb869..3fbf129 100644
--- a/exchange/js/exchange.js
+++ b/exchange/js/exchange.js
@@ -289,6 +289,7 @@ function setBurnTokensHeaderText(text = '', visible = false) {
 
   header.textContent = visible ? text : '';
   header.style.visibility = visible ? 'visible' : 'hidden';
+  header.style.display = visible ? 'block' : 'none';
 }
 
 function setDropdownState(dropdown, { maxQty = 0, enabled = false } = {}) {
